import { getRenderSettingDefinitionsByGroup } from "../../contracts/render_settings/index.js";

export const FRACTION_CONTROL_DEFINITIONS = Object.freeze(
    getRenderSettingDefinitionsByGroup("fractions")
);
