import { getRenderSettingDefinitionsByGroup } from "../../contracts/render_settings/index.js";

export const ROOT_CONTROL_DEFINITIONS = Object.freeze(
    getRenderSettingDefinitionsByGroup("roots")
);
