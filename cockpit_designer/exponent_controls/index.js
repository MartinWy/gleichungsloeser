import { getRenderSettingDefinitionsByGroup } from "../../contracts/render_settings/index.js";

export const EXPONENT_CONTROL_DEFINITIONS = Object.freeze(
    getRenderSettingDefinitionsByGroup("exponents")
);
