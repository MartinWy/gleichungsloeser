# Status 2026-07-19

Status: aktiv  
Stand: Sonntag, 19. Juli 2026

## Kurzfassung
- Der aktuelle Arbeitsstand ist in `main` auf Commit `3ee1701` gesichert.
- Das Cockpit ist als produktive Huelle aktiv und baut auf demselben Renderpfad auf wie PDF und Arbeitsblatt.
- Der Gleichungsloeser arbeitet weiterhin nach dem Standard: `core/` ist die semantische Wahrheit, nicht die UI.
- Der Gamma-Fall des Kosinussatzes ist im Code aktuell verifiziert; ein zwischenzeitlich beobachteter Rueckfall kam von alten laufenden Serverprozessen und nicht von einem Rollback im Repository.
- Die operative Anleitung fuer das bisherige Cockpit auf lokalem Server mit echter Renderseite steht in `docs/Projektstand/COCKPIT_RENDER_RUNBOOK.md`.

## Produktiver Stand
- Einstieg: `index.html`
- Cockpit-Huelle: `cockpit.js`, `cockpit.css`, `scripts/cockpit_server.mjs`
- Render- und Projektionspfad:
  - `core/P4_Projektion/`
  - `components/Arbeitsblatt_Druckansicht/viewModel.js`
  - `components/Arbeitsblatt_Druckansicht/renderKernelCore/`
  - `components/Arbeitsblatt_Druckansicht/columnLayoutCore/`
  - `scripts/export_projection_pdf_core/`

## Was heute als gesichert gilt
- LaTeX ist nicht mehr die Layoutwahrheit, sondern Zeichensatz- und Exporttraeger.
- Spaltenlogik, sichtbare Shells und positionstreue Setzung kommen aus der eigenen Logik.
- Das Cockpit arbeitet auf demselben produktiven Renderpfad wie Vorschau und PDF.
- Griechische Variablen koennen sowohl ausgeschrieben als auch als Unicode-Zeichen eingegeben werden.
- Schrittfarben koennen ueber den Cockpit-Pfad in die gerenderte Ausgabe uebergeben werden.

## Verifizierte Checks am 19. Juli 2026
- `tests/active/cosine_law_gamma.test.js`
- `tests/active/cockpit_color_targets.test.js`
- `tests/active/input_adapter.test.js`
- `tests/active/latex_p4_export.test.js`

## Wichtige Betriebsregel
Wenn die sichtbare Ausgabe aelter wirkt als der Codezustand, muss zuerst der laufende Cockpit-Server geprueft werden.

Praktische Regel:
- `4173` ist der Standardpfad fuer den lokalen Cockpit-Server.
- weitere Ports wie `4182` sind nur parallele lokale Instanzen und keine normative Referenz
- `.cockpit-preview/` ist reine Laufzeit-Ausgabe und kein normativer Projektstand

## Ergaenzung vom 21. Juli 2026
Die bisher gelebte Cockpit-Arbeitsform ist jetzt ausdruecklich dokumentiert:

- lokaler Server auf `4173`
- Eingabeseite rechts
- gerenderte Produktansicht links
- Gleichung eingeben, Farben setzen, Ergebnis live beobachten

Referenz:

- `docs/Projektstand/COCKPIT_RENDER_RUNBOOK.md`

## Naechste Linie
1. Projektdokumentation auf den heutigen Stand halten
2. Genesis-Maschine auf den aktuellen Render- und Cockpit-Standard nachziehen
3. Genesis-Mensch als verstaendliche Begleitfassung pflegen
4. das Cockpit schrittweise erweitern, ohne eine zweite mathematische oder visuelle Wahrheit einzufuehren
